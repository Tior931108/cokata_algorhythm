class Solution {
    public int solution(String s) {
        // 문자열을 정수형으로 변환
        int number = Integer.parseInt(s);
        // int number = Integer.valueOf(s);
        int answer = number;
        return answer;
    }
}